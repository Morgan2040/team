#ifndef MAIN_H
#define MAIN_H

/* The C Standard library header files */
#include <stdarg.h>
#include <unistd.h>

/* The Prototypes of Functions in program */
int _putchar(char chr);
int _put_string(char *string);
int _printf(const char *format, ...);
void gt_int(int numb);

#endif /* MAIN_H */
